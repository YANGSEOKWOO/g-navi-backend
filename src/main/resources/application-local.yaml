spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/gnavidb
    username: gnavi_user
    password: gnavipwd
    driver-class-name: org.postgresql.Driver
  data:
    mongodb:
      host: localhost
      port: 27017
      database: gnavi
#fastapi:
#  base-url: http://localhost:8000
#  endpoints:
#    chat-room-create: /ai/room/create
#    message-send: /ai/message/send
#  timeout:
#    connect: 5000
#    read: 30000
#  retry:
#    max-attempts: 3
#    delay: 1000